#include <stdio.h>

int main() 
{
    int c, i;
    printf("Enter a number: ");
    scanf("%d", &c); 
    for (i = 1; i <= 10; i++)
    {
        printf("%d * %d = %d\n", c, i, c * i);
    }
    return 0; 
}
